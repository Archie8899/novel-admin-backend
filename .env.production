# 生产环境变量示例
# Railway 会自动注入 DATABASE_URL（PostgreSQL）

# Server
PORT=3000
NODE_ENV=production

# JWT（请在 Railway Dashboard 中设置）
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS（部署后替换为你的前端地址）
CORS_ORIGIN=https://your-frontend.railway.app
